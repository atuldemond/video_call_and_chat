/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./views/**/*.ejs"], // Correctly targets all EJS files in the views directory and its subdirectories
  theme: {
    extend: {},
  },
  plugins: [],
};
